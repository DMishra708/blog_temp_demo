<%= form_for(@recepy) do |f| %>
  <% if @recepy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recepy.errors.count, "error") %> prohibited this recepy from being saved:</h2>

      <ul>
      <% @recepy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
